# DeepPatient Marketing Frontend

Vite, React, and TypeScript frontend for the public DeepPatient marketing website.

```bash
npm install
npm run dev
npm run lint
npm run build
```

During development, Vite proxies `/api` to `VITE_API_PROXY_TARGET`, which defaults to `http://127.0.0.1:8000`.
